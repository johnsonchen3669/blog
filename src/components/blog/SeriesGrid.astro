---
import Link from '~/components/base/Link.astro'
import type { BlogSeriesSummary } from '~/utils/blog'

const { items } = Astro.props as { items: BlogSeriesSummary[] }
---

<div class="field-series-grid">
  {
    items.map((item) => (
      <Link class="field-series-card" href={item.path}>
        <span class="field-series-card__count">
          {String(item.count).padStart(2, '0')} notes
        </span>
        <h3>{item.title}</h3>
        <p>{item.description}</p>
        <span class="field-series-card__action">進入系列 →</span>
      </Link>
    ))
  }
</div>
